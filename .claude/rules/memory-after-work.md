After completing a task that involved decisions, preferences, or bug fixes:

1. Store the outcome in Vestige (`smart_ingest`) — decisions fade naturally if not referenced again
2. If reference material was created (docs, standards, samples), also store in Qdrant — permanent library
3. If working in a Serena-activated project, update session state via Serena `write_memory`
